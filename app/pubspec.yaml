name: resumidor_ia
description: "Resumidor de clases con IA"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  go_router: ^14.0.0
  http: ^1.2.0
  flutter_markdown: ^0.7.4
  shared_preferences: ^2.3.0
  flutter_secure_storage: ^9.2.2
  dio: ^5.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true